# Optional. Raises the GitHub API rate limit when analyzing arbitrary public
# repositories. The bundled demo storefront needs no network access and no key,
# so judges can run the whole pipeline without configuring anything.
GITHUB_TOKEN=
